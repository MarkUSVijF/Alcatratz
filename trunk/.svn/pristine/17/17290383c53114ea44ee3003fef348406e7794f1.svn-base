/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package net.technikumwien.bic4b18_01.server.local;

import java.io.IOException;
import net.technikumwien.bic4b18_01.server.connectionMW.ConnectionOUT;
import java.net.MalformedURLException;
import java.rmi.RemoteException;
import java.util.logging.Level;
import java.util.logging.Logger;
import net.technikumwien.bic4b18_01.common.assist.TraceHelper;
import net.technikumwien.bic4b18_01.common.settings.LoggerSettings;
import static net.technikumwien.bic4b18_01.common.assist.TODO.MORE;
import spread.SpreadException;

/*
        IClient2ServerLogic registerLogic = ConnectionLogic.getInstance();
        
        System.out.println("Starting up clientside service...");
        registerLogic.construct(); //creates a connection 2 the lobby server
        System.out.println("clientside service up and running...");
 */
/**
 *
 * @author Florian
 */
public class ServerApplication {

    private static final Logger logger = Logger.getLogger(TraceHelper.getClassName());

    public static void main(String args[]) {
        try {
            LoggerSettings.initLogging();
        } catch (IOException ex) {
            logger.log(Level.SEVERE, "logger could not be started", ex);
        }

        try {
            ConnectionOUT.construct();
        } catch (RemoteException | MalformedURLException ex) {
            logger.log(Level.SEVERE, "server could not be set up", ex);
            return;
        } catch (SpreadException ex) {
            logger.log(Level.SEVERE, "some spread problem", ex);
        }

        MORE.todo(ServerApplication.class.getName());
    }
}
